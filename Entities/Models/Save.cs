namespace Entities.Models
{
    // Represents a single save point within a backup
    public class Save : Entity
    {
        public Save()
        {
            // Currently no additional initialization logic
        }
    }
}
